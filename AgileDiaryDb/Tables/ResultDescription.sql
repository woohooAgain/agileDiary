CREATE TABLE [dbo].[ResultDescription]
(
	[Id] UNIQUEIDENTIFIER NOT NULL PRIMARY KEY, 
    [Achievment] TEXT NOT NULL, 
    [Thanks] TEXT NOT NULL, 
    [PossibleImprovements] TEXT NOT NULL, 
    [Lesson] TEXT NOT NULL
)
