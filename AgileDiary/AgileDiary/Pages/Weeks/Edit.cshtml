@page
@model AgileDiary.Pages.Weeks.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Week</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Week.WeekId" />
            <div class="form-group">
                <label asp-for="Week.StartDate" class="control-label"></label>
                <input asp-for="Week.StartDate" class="form-control" />
                <span asp-validation-for="Week.StartDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Week.SprintId" class="control-label"></label>
                <select asp-for="Week.SprintId" class="form-control" asp-items="ViewBag.SprintId"></select>
                <span asp-validation-for="Week.SprintId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
