@page
@model AgileDiary.Pages.Weeks.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Week[0].StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Week[0].Sprint)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Week) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sprint.SprintId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.WeekId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.WeekId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.WeekId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
