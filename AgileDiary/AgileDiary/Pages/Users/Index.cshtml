@page
@model AgileDiary.Models.PageModels.Users.IndexUsersPageModel
@{
    ViewData["Title"] = "Sprints";
}


<h2>Users</h2>

<a asp-page="Create" >Create user</a> |

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users[0].Role)
            </th>
            <th>
                Actions
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.Users)
        {
            var d = new Dictionary<string, string>
    {
            {"userId", user.Id}
        };
    <tr>
        <td>
            @user.Id
        </td>
        <td>
            @user.Email
        </td>
        <td>
            @user.Role
        </td>
        <td>
            <a asp-page="MakeAdmin" asp-route-id="@user.Id" asp-all-route-data="d">Make admin</a> |
            <a asp-page="Delete" asp-route-id="@user.Id" asp-all-route-data="d">Delete</a>
        </td>
    </tr>
}
</table>